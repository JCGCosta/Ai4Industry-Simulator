# Spring Proxy for the AI4industry Hackathon


### To Run the Project

To run the application use:
```
./gradlew bootRun
```

To build the application use:
```
./gradlew build
```

To assemble the application and create the Jars use:
```
./gradlew assemble
```
